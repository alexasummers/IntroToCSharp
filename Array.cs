using System;

namespace Arrays
{
  class Arrays
  {
    static void Main(string[] args)
    {
      string[] classes = {"GAME102", "GAME104", "GAME121", "GAME131"};
      for (int i = 0; i < classes.Length; i++) 
      {
        Console.WriteLine(classes[i]);
      }
    }
  }
}
